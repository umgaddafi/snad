<IfModule mod_rewrite.c>
    RewriteEngine On

    # Allow direct access to test scripts
    RewriteCond %{REQUEST_URI} ^/db_test\.php [NC]
    RewriteRule ^ - [L]

    # Route /api/ requests to the Laravel Backend
    RewriteCond %{REQUEST_URI} ^/api/ [NC]
    RewriteRule ^api/(.*)$ backend/public/index.php/$1 [L]

    # Route all other requests to React's index.html (for React Router)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.html [QSA,L]
</IfModule>
